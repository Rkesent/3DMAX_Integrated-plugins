# 错误日志
- 2023-10-05: 修正 `getSubMaterialIDs` 函数中的 `else` 语句语法错误。

# 修改日志

## 2023-10-05
- 实现了检查丢失贴图的功能
- 更新了检查逻辑以检测选中模型的材质球索引的贴图路径是否存在
- 优化了检查逻辑以提高运行效率

### 修改日志

#### 2023-10-05
- 修复了 `Add` 方法参数不匹配的问题，将 `map[3]` 转换为字符串后再添加。
- 优化了检查丢失贴图的速度，使用批处理减少UI更新。
- 实现了分批处理模型，避免一次性处理大量数据导致的卡死。
- 优化了材质检查逻辑，减少不必要的检查。
- 使用缓存避免重复检查材质。
- 修复了 `append` 函数参数格式问题，使用 `#()` 语法构建字典。
- 使用 `dotNet` 处理 JSON 序列化和反序列化。
- 加载 `Newtonsoft.Json` 库。
